version: '3.8'

services:
  blog-service:
    build:
      context: ./blog-service
    container_name: blog-service
    environment:
      - BLOG_DATABASE_PATH=/data/blog.db
    ports:
      - "8005:8005"
    volumes:
      - blog_data:/data

volumes:
  blog_data:
