# Example environment variables for deployment
# Copy this to .env and edit values as needed before running docker compose

# Path inside the container where SQLite DB will be stored (persisted via volume)
BLOG_DATABASE_PATH=/data/blog.db

# Token expiration in days
TOKEN_EXP_DAYS=30

# Host/Port expose (docker-compose maps 8005:8005)
HOST=0.0.0.0
PORT=8005

# Any other envs you may want to set
# e.g. ADMIN_EMAIL=admin@example.com
