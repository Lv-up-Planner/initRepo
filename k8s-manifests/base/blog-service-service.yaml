apiVersion: v1
kind: Service
metadata:
  name: blog-service
  labels:
    app: blog-service
spec:
  type: ClusterIP
  selector:
    app: blog-service
  ports:
  - protocol: TCP
    port: 8005
    targetPort: http